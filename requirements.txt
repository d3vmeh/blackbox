# Blackbox backend (P1/P2/P4). Frontend deps live in package.json.
# Create a venv and: pip install -r requirements.txt

# --- core (everyone) ---
pydantic>=2.6

# --- agent + replay (P1) ---
anthropic>=0.40           # subject agent's reasoning nodes (claude-opus-4-8)
python-dotenv>=1.0
redis>=5.0
langgraph>=0.2            # for the checkpointed replay path (Owner C)
langchain-core>=0.3

# Browserbase-powered web tool (P1). Connects to the REMOTE browser over CDP,
# so you do NOT need `playwright install` — the python package is enough.
browserbase>=1.0
playwright>=1.40

# --- API (P4) ---
fastapi>=0.115
uvicorn>=0.30

# --- tracing for the Arize track: P1 emits spans, view in Phoenix ---
# Optional — `agent/otel.py` no-ops gracefully if these are absent.
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
arize-phoenix>=5.0        # run `phoenix serve`, open http://localhost:6006
